{
  "name": "yggdrasil-design-system",
  "version": "0.0.0",
  "private": true,
  "description": "Design system & component workshop for Yggdrasil. Storybook renders the exact CSS the app ships \u2014 no parallel reimplementation. The app itself stays a zero-dependency single HTML file built by build/build.py; everything here is dev-only tooling.",
  "type": "module",
  "scripts": {
    "storybook": "storybook dev -p 6006 --no-open",
    "build-storybook": "storybook build -o storybook-static"
  },
  "devDependencies": {
    "@fontsource-variable/hanken-grotesk": "^5.3.0",
    "@storybook/addon-a11y": "^10.5.5",
    "@storybook/addon-essentials": "^8.6.0",
    "@storybook/web-components-vite": "^10.5.5",
    "lit": "^3.2.0",
    "storybook": "^10.5.5",
    "vite": "^5.4.0"
  },
  "engines": {
    "node": ">=22"
  }
}
